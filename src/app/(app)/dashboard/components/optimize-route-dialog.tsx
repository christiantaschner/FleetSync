

"use client";

import React from 'react';

// This component is no longer in use, as the simple route optimization feature
// has been replaced by the more intelligent "Resolve" workflow triggered from
// the Schedule View. It is kept to avoid breaking file-based updates but can be
// removed in a future cleanup.

const OptimizeRouteDialog = () => {
    return null;
};

export default OptimizeRouteDialog;
